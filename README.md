# linux-apollo
Hardened Linux LTS (v6.12.x) kernel patch-set. Includes patches from linux-hardened, linux-openPaX, GrapheneOS and custom patches that apply sane defaults and implement AppArmor user namespace restrictions (ported from Ubuntu's questing-quokka kernel).
